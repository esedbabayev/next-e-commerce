/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["example.com", "res.cloudinary.com"], // Add your external image domains here
  },
};

export default nextConfig;
